# ── 環境変数テンプレート (.env.example) ──

# LINE WORKS のログイン情報
LINEWORKS_ID=your.lineworks.id@example.com
LINEWORKS_PASS=yourLineWorksPassword

# ELYZA モデルのローカルパス（現在未使用 - 将来の機能拡張用）
# ELYZA_MODEL_PATH=/path/to/models/elyza7b/ELYZA-japanese-Llama-2-7b-instruct.Q4_0.gguf

# Headless Chrome 実行バイナリのパス（macOS標準）
CHROME_BINARY=/Applications/Google Chrome.app/Contents/MacOS/Google Chrome

# ChromeDriver の実行ファイルパス（Homebrewでインストールした場合）
CHROMEDRIVER_PATH=/usr/local/bin/chromedriver

# 注意事項:
# 1. LINEWORKS_ID と LINEWORKS_PASS は必須です
# 2. ChromeDriverのバージョンはChromeのバージョンと互換性が必要です
# 3. パスにスペースが含まれる場合は引用符で囲む必要はありません
